数学运算：
double c=double(0)/double(0)输出c的值显示无穷大infinity
10+21x21-10=0 441
&&只要有以个为假，就是假的，后面的就不运行了。
Java逻辑运算一定时Boolean类型的值，js不一定

int[] a={1,2,3};
int[] b=new int[5];
int[] c=new int[]{1,2,3};
没有赋值的数组元素默认时0；
长度声明以后就不可以改变

